cron:
- description: old data cleanup
  target: backend
  url: /restricted/cleanup/trigger/old
  schedule: every 9 minutes

- description: Trim lost files on GS that are not referenced by a ContentEntry anymore.
  target: backend
  url: /restricted/cleanup/trigger/trim_lost
  schedule: every day 4:00

- description: Cron job that gathers statistics
  target: backend
  url: /restricted/stats/update
  schedule: every 1 minutes

- description: Mail exception report via ereporter2
  target: backend
  url: /restricted/cron/ereporter2/mail
  schedule: every 1 hours synchronized
