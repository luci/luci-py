cron:
- description: old data cleanup
  url: /restricted/cleanup/trigger/old
  schedule: every day 01:50

- description: testing data cleanup
  url: /restricted/cleanup/trigger/testing
  schedule: every day 09:50

# TODO(maruel): It's currently broken, it times out while enumerating the
# BlobStore entries at around 4000 entities within 60s. So a cursor query should
# be done.
#- description: orphaned blobstores cleanup
#  url: /restricted/cleanup/trigger/orphaned
#  schedule: every day 17:50
