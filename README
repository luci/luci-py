Contains the AppEngine code to store .results files and test data generated by
trunk/src/tools/isolate/isolate.py.

To test this code, first upload the changes to app engine as version test, and
then run main_test.py.